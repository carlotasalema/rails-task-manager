<h1><%= @task.title %></h1>
<h2><%= @task.details %></h2>
<p><%= @task.completed ? "This task is completed" : "This task is not completed yet" %></p>
<%= link_to "Back to list", tasks_path%>
<%= link_to "Edit task", edit_path%>
<%= link_to "Delete task", task_path, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
